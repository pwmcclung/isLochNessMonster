function isLockNessMonster(s) {
  //FIND THE LOCH NESS MONSTER. SAVE YOUR TREE FIDDY
  let one = "tree fiddy"
  let two = "3.50"
  let three = "three fifty"
  if (s.includes(one)|| s.includes(two)|| s.includes(three)){
    return true
  }else{
    return false
  }
}
